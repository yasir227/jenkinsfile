pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'python -V'
            }
        }
    }
 post {
        always {
            sh 'Always'
        }
        changed {
            sh 'Only if status changed from Success to Failure or vice versa w.r.t. last run.'
        }
        success {
            sh 'Only on SUCCESS'
        }
        unstable {
            sh 'Only if run is unstable'
        }
        failure {
            sh 'only on Failure'
        }
    }
}
